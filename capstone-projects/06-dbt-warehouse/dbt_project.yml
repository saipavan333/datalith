name: datalith
version: "1.0"
config-version: 2
profile: datalith

model-paths: ["models"]
seed-paths: ["seeds"]
target-path: "out/target"
clean-targets: ["out/target", "dbt_packages"]

models:
  datalith:
    staging:
      +materialized: view
    marts:
      +materialized: table
